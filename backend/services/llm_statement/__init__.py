"""LLM-first bank-statement extraction with deterministic validation.

The package intentionally has no eager imports so the existing application can
start without optional provider dependencies while the feature flag is off.
"""
